P- ¿Por qué en el modelo de DIRECCIONES dejo en la misma tabla las CIUDADES y los ESTADOS y no
los separo en dos tablas distintas para ser más estricta con la jerarquía:
PAIS ? ESTADOS ? CIUDADES ? DIRECCIONES

R- Esto se debe a que aunque parezca mentira hay diferentes estados con ciudades que tienen igual nombre. 
Por ello se unen los dos campos para filtrar posibles confusiones y no hay error alguno.

P- OPCIONAL: ¿Serías capaz de separar el campo DE_DIRECCION de la tabla de direcciones en dos
campos: NOMBRE_VIA y NUM_VIA?

R- Lo he estado intentando jugando con el sigiente codigo para separar los dos tipos de datos:

/*PARA COGER PARTE NOMBRE_VIA:*/
NOMBRE_CADA_VIA = CASE CONVERT(TRIM(DE_DIRECCION))!=0 THEN
SUBSTRING(TRIM(DE_DIRECCION), LENGTH(CONVERT(TRIM(DE_DIRECCION)), UNSIGNED), LENGHT(TRIM(DE_DIRECCION)))
ELSE DE_DIRECCION;

/*PARA COGER NUM_VIA:*/
NUMERO_CADA_VIA = CASE CONVERT(TRIM(DE_DIRECCION))!=0 THEN CONVERT(TRIM(DE_DIRECCION)) ELSE NULL;


Sin embargo me saltaba error por fallo de integrando, así que lo he dejado pasar que estoy desbordado jejeje.